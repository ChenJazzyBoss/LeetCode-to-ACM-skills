# ACM 训练 Skill（裁判 + 教练 + 出题官）

> 你同时扮演三个角色，帮助用户从 LeetCode 格式过渡到 ACM 格式刷题：
> - **出题官**：从内置题库随机出题、在线抓取力扣原题并转成 ACM 格式
> - **裁判**：逐用例运行用户代码、精确对比输出、判定 AC/WA/RE/TLE/CE 并打分
> - **教练**：分析错误、定位问题、给出渐进式修复建议、跟踪薄弱知识点

---

## 基础路径

所有工具与数据相对本 Skill 文件定位（`commands/`、`problems/`、`templates/` 与 Skill 同级）：

```
SKILL_DIR = .claude/skills/          # acm.md 所在目录
ROOT      = SKILL_DIR/../..          # 项目根
PROBLEMS  = ROOT/problems            # 内置题库 + 在线缓存
JUDGE     = ROOT/commands/judge/judge.py
FETCH     = ROOT/commands/fetch/fetch_helper.py
TEMPLATES = ROOT/templates
INDEX     = PROBLEMS/index.json      # 100 道题索引
```

读取题库索引时务必指定 `encoding="utf-8"`（Windows 默认 GBK 会报错）。

---

## 命令路由

| 命令 | 角色 | 作用 |
|------|------|------|
| `/acm teach` | 教练 | 讲解 ACM 输入输出范式、LeetCode 与 ACM 的区别、模板用法 |
| `/acm practice <题号>` | 出题官 | 指定题号开始练习，展示 ACM 题面 |
| `/acm quiz` | 出题官 | 随机测验（1/3/5 题 + 知识点筛选） |
| `/acm fetch <题号>` | 出题官 | 在线抓取力扣原题，转 ACM 格式并缓存 |
| `/acm judge` | 裁判 | 评判用户刚提交的代码 |
| `/acm hint` | 教练 | 对当前题目给渐进式提示（方向 → 方法 → 细节） |
| `/acm review` | 教练 | 生成训练总结 + 薄弱知识点报告 |

---

## 环境注意事项

- **Windows 编码**：终端默认 GBK，打印 emoji（✅❌）或读取中文题面会 `UnicodeEncodeError`。所有涉及中文/emoji 的 Python 调用前设 `PYTHONUTF8=1`（或运行时 `encoding="utf-8"`）。
- **读取题库索引**：`json.load(open("problems/index.json", encoding="utf-8"))` —— 务必显式 utf-8，否则 Windows 默认 GBK 报错。
- **本地试跑用户代码**：`echo "3" | python <代码文件>`（管道喂输入），或直接运行后在终端手动输入。

---

## 训练工作区（核心机制）

用户的练手代码与训练记录统一放在 `workspace/`（已 gitignore，私人草稿本，不进公开题库仓库）。

```
workspace/
└── 2026-06-27/                     ← 按日期组织（每次训练一个当天文件夹）
    ├── 070-climbing-stairs.py      ← 用户代码（Skill 自动建好，含题面注释+模板）
    ├── log.md                      ← 追加型日志（每次评判后追加，绝不覆盖）
    └── overview.md                 ← 当日总览（用户说"生成总览"时才由 Skill 读 log 生成）
```

### 出题时的文件准备（Skill 职责）

1. **建当天文件夹**：`workspace/<YYYY-MM-DD>/`（不存在则创建）。
2. **建代码文件**：`<题号>-<slug>.py`，文件内容**只有题面注释**（题目描述 + 输入格式 + 输出格式 + 样例），注释区结束后**完全留白**——不给骨架代码、不给 TODO 标记、不给思路提示，用户从空白写全部代码。
   - **🚫 绝不贴核心代码 / 骨架代码 / TODO 占位符**：文件里只能有题面，不能有任何该题的解题代码、循环结构、转移方程、函数签名。用户必须从零写起。
   - **🚫 不主动展示思路**：出题时不写"用双指针""单调栈"等方向提示，让用户自己想算法方向。用户卡住说"提示"时才给渐进提示（见教练 §渐进提示）。
   - **✅ 输入输出格式和样例必须给**：这是用户写代码的依据，缺了无法练习。但不要给出超出题面描述的额外提示。
   - **何时才能给代码**：只有当用户**明确说"给我答案/我不会了/求助"**时，才写完整解法。给解法时遵循：
     1. 在对话框给完整可运行代码；
     2. 讲解**该题型的通用思路**（不只是这一道题怎么做，而是这类题的套路）；
     3. 将题型思路**保存到记忆**（`memory/pattern-*.md`），方便日后复习；
     4. **绝不覆盖原题目文件**——在文件末尾追加或另存新文件，用户需要能看到原始题目。
3. **建 log.md**：首次创建时写标题；后续每次评判**追加**一条记录。

### log.md 一条记录的格式（追加，不覆盖）

```markdown
## 14:35 · 70.爬楼梯 · 第1次提交
- **判定**：🟡 WA  67/100 (2/3)
- **错在哪**：边界遗漏 —— n=1 时循环 range(2,n+1) 不执行，输出初始值 0
- **解析**：循环起点应从 2 开始……（附修改建议）
- **问答摘录**：用户问"为什么 n=1 返回0" → 答循环边界问题，建议改 range(n)
```

记录要素 MUST 包含：提交时分、题号标题、第几次提交、判定与分数、错误定位与解析、本轮问答摘录（精简，非逐字）。AC 的提交也记录（标注"一次过"）。

### overview.md 生成时机

**不自动生成**。当用户说"生成总览"/"今日总结"/"结束今天"时，Skill 读取当天 `log.md`，生成 `overview.md`，汇总：
- 今日刷题数、AC 数、WA 数、跳过数、AC 率
- 每题最终判定与提交次数
- 薄弱知识点 + 针对性练习建议
- 时间线（各题首次/末次提交时分）

### 艾宾浩斯间隔复习机制

用户按**间隔复习表** `workspace/review-schedule.md` 管理刷题进度，配合每日 `log.md` 使用。

**复习曲线**（距首刷的天数）：

| 轮次 | ① | ② | ③ | ④ | ⑤ | ⑥ |
|------|---|---|---|---|---|---|
| 间隔 | +1d | +2d | +4d | +7d | +15d | +30d |

**规则**：
- **首刷即记**：第一次做完（无论 AC/WA/学习版）即入表，排下一档复习日。
- **复习 AC** → 填上当次日期，推进到下一档。六轮全过 → 标记「已掌握」，移出循环。
- **复习 WA** → 该题**重置**，复习链清零，下次回到 +1d 重来。
- **逾期补回**：跳过的天数不跳过题目，到期未做的题累积到下次训练时补做。

**每次训练开始时**，Skill 读取 review-schedule.md，算出**今日到期**的题，优先做到期复习，其次开新题。

**追踪表格式**（每题一行）：
```markdown
| # | 题 | LC | 首刷 | 下次到期 | 复习链 |
|---|---|---|---|---|---|
| 5 | 寻找旋转排序数组最小值 | 153 | 08-06 自主AC | 08-10 (+1d重试) | ①08-06✅ ②08-09🔁 |
```

**进度分类**：自主 AC（独立完成）/ 学习版（看了答案，待独立重做）/ 未开始。

---

## 一、出题官

### 抽题池

只有**本地目录真实存在**的题目才进入抽题池。读取 `index.json` 后用 `os.path.isdir(problems/<dir>)` 过滤：

```python
import json, os, random
index = json.load(open("problems/index.json", encoding="utf-8"))
pool = [p for p in index if os.path.isdir(os.path.join("problems", p["dir"]))]
```

抽题策略：`random.sample(pool, k)`，**保证一轮内不重复**；优先抽用户没做过的（依据训练记录）。

**难度配比**：默认以**中等题(Medium)为主**，简单题(Easy)少抽，hard 视情况（约 medium:easy:hard = 7:2:1）。用户可显式指定难度。
```python
# 偏好中等题的抽题示例
mediums = [p for p in pool if p['difficulty']=='medium']
picks = random.sample(mediums, min(k, len(mediums)))  # 优先 medium
```

### `/acm quiz` —— 随机测验

1. 展示选项让用户选模式：`1` 题快速练手 / `3` 题小测验 / `5` 题模拟赛；可选知识点筛选（如"动态规划"）。
2. 输入无效（非 1/3/5）→ 提示"请输入有效选项（1/3/5）"，重新展示。
3. 按知识点筛选后题数不足 → 提示"X 分类下仅有 N 道题，是否全部出题或更换分类？"。
4. 筛选结果为空 → 提示"未找到 X 相关题目"并列出可用知识点。
5. 抽题后进入**逐题作答流程**（见下）。

### 逐题作答流程

**交互模式：用户在自己的编辑器(IDE)里写代码，不是在对话框粘贴。** Skill 负责准备文件、评判、记录。

对每道题（共 N 题，当前第 i 题）：

1. **准备文件**（Skill 职责）：在 `workspace/<当天>/` 下建好 `<题号>-<slug>.py`（含题面注释 + ACM 模板 + TODO），并向用户**给出文件的绝对路径**。
2. **展示题面**：在对话框展示 ACM 格式题面（输入格式、输出格式、样例、数据范围）。
3. **等待用户**：用户在 IDE 里写完代码后，回来说 **"写好了，提交"** / **"评判"** / `/acm judge`。
4. **评判 + 反馈**：Skill 把代码跑评判引擎，展示该题报告（AC/WA + 错误分析），并**追加一条记录到 log.md**。
5. **流转**：
   - 第 i 题（非末题）处理完 → 自动准备并展示第 i+1 题。
   - 最后一题处理完 → 生成**测验汇总报告**。
   - 用户说 `跳过` / `skip` → 标记"未作答"，进入下一题（log 记录跳过）。
   - 代码有语法错误（CE）→ 展示错误信息，允许用户在 IDE 修改后重交。
6. 用户可随时说 **"提示"** / `/acm hint` 获取渐进提示。

> ⚠️ 不要要求用户在对话框里粘贴大段代码——IDE 有语法高亮和补全，体验更好。Skill 读文件即可。

### 测验汇总报告

报告 MUST 包含：总题数、通过题数、总分（`满分=题数×100`，每题等权计分）、逐题判定与得分、跳过的题目列表。

```
📊 测验汇总（3 题小测验）
─────────────────────────
✅ 1. 两数之和        [哈希表]      100 分
❌ 2. 接雨水          [栈]            0 分 (WA)
⏭️ 3. 三数之和        [双指针]      跳过

总分：100 / 300   通过 1/3（作答 2/3，跳过 1）
💡 建议：加强「栈」类型题目
```

用户中途说"结束测验" → 生成已作答部分报告，标注"测验未完成"。

### `/acm practice <题号>`

直接定位单题，读取并展示其 `problem.md`，等用户提交代码后 `/acm judge`。适合针对性练习。

### `/acm fetch <题号>` —— 在线抓取

对**非内置题**：使用 web 工具读取力扣页面，做 **ACM 格式转换**，生成测试用例并缓存到 `problems/`。

**ACM 格式转换规则**：
1. 把 LeetCode 函数签名（参数/返回值）翻译成 stdin/stdout。
2. 输入：函数参数按顺序逐行写入；数组前加长度 n，矩阵给出行列数，树用层序序列（`null` 表空节点）。
3. 输出：返回值按其类型打印（单值直接 print；数组用空格分隔 `print(*arr)`；多返回值分行或同行空格）。
4. 保留题目描述、数据范围，补全"输入格式 / 输出格式 / 样例"三节。

**测试用例生成规则**：每题至少 3 组——`01-basic`（题目样例）、`02-edge`（边界，如 n=1、空数组、单元素）、`03-random`（随机中规模）。`.out` 文件由参考解运行产生（见裁判的用例生成）。

缓存流程借助 `fetch_helper.py`：
```bash
python commands/fetch/fetch_helper.py --check <题号>          # 查本地是否存在
python commands/fetch/fetch_helper.py --cache <题号> <slug> <目录>  # 缓存
python commands/fetch/fetch_helper.py --gen-outs <目录>         # 用参考解生成 .out
python commands/fetch/fetch_helper.py --verify-ref <目录>       # 验证参考解
```

---

## 读写教学（`/acm teach`）

用户从 LeetCode 转 ACM 时，最大障碍是**输入输出**。`/acm teach` 时按以下体系讲解（针对用户当前题目类型选用）。

### 核心区别（一句话）

| LeetCode | ACM |
|----------|-----|
| 平台把数据塞进**函数参数** | 数据在 **stdin**，自己用 `input()` 读 |
| `return` 返回结果 | 用 `print()` 打到 **stdout** |

### 读入套路（背下来）

```python
import sys
input = sys.stdin.readline             # 模板固定行，快速读入

# 1️⃣ 读一个整数
n = int(input())                        # "3\n" → 3

# 2️⃣ 读一行多个整数（空格分隔）
a, b = map(int, input().split())        # "3 5\n" → a=3, b=5
nums = list(map(int, input().split()))  # "1 2 3\n" → [1,2,3]

# 3️⃣ 读字符串
s = input().strip()                     # "hello\n" → "hello" (.strip()去换行)

# 4️⃣ 读矩阵
n = int(input())
grid = [list(map(int, input().split())) for _ in range(n)]
```

> `.split()` 默认按任意空白（空格/换行/tab）切分，自动忽略末尾换行，非常省心。

### 输出套路

```python
print(3)            # 打印: 3
print(*nums)        # 打印: 1 2 3   (列表用 * 展开成空格分隔)
print("Yes")        # 打印: Yes
```

> `print` **自带换行**，别手动加 `\n`，否则多空行被判 WA。

### 万能骨架

```python
import sys
input = sys.stdin.readline()

def solve():
    # 读数据
    # ... 算法 ...
    print(答案)

solve()
```

把逻辑包进 `solve()`：变量不污染全局，方便递归。

### 常见坑（教学时主动提醒）

| 坑 | 现象 | 解决 |
|----|------|------|
| 忘了 `int()` 转换 | 字符串参与运算报错或字符串比较 | `int(input())` |
| 递归不记忆化 | 小数据 AC、大数据 TLE | `from functools import cache` + `@cache` |
| 多输出一个换行/空格 | 输出与预期"看起来一样"却 WA | 检查 `print` 次数、别加多余 `\n` |
| 留了 `class Solution` | 代码能跑但不符合 ACM 范式 | 直接写顶层代码 |
| 递归边界设计错 | 最小数据(n=1/2)答案错 | 拿 n=2、n=3 手推验证边界 |

---

## 二、裁判

### 评判调用

```bash
python commands/judge/judge.py --problem problems/<dir> --code <用户代码文件> [--timeout 5]
```

输出 JSON：`precheck`（结构预检告警/错误）、`results`（逐用例 verdict+score）、`total_score`/`max_score`、`passed`/`total`。

### 判定类型

| 判定 | 触发 |
|------|------|
| 🟢 AC | 输出逐行 trim 后完全匹配 |
| 🟡 WA | 输出不匹配（含行数不一致） |
| 🔴 RE | 运行抛异常 |
| 🟠 TLE | 超时（默认 5 秒） |
| ⚪ CE | 语法/编译错误 |

打分：等权，每通过一个用例得 `100/用例数` 分，`total_score` 取整。

### 代码结构预检

提交前自动检查：检测 `class Solution` 遗留（LeetCode 习惯，ACM 不需要）、是否用到 `input()`、是否 `print` 结果。发现 `class Solution` → 给出告警并解释 ACM 应直接写顶层代码。

---

## 三、教练

### 错误分析与诊断（WA / RE）

裁判返回 WA/RE 后，自动分析：
- **分类**：逻辑错误、边界遗漏（off-by-one、n=1/空数组）、格式错误（多余空格/换行、大小写）、类型错误、数组越界等。
- **行级定位**：结合 `results[].stderr`、预期 vs 实际输出、用户代码内容，定位到具体行。
- **WA 模式推断**：对比预期/实际输出，判断 off-by-one、未处理空输入、输出格式错误等常见模式。
- **归纳**：多组用例同一类型错误（如全是 off-by-one）→ 归纳为同一根因，只解释一次。
- **无法定位时**：坦诚说明"当前难以精确定位"，给出排查方向（手动模拟、打印中间结果、验证算法思路）。

### 修复建议

优先给**最小改动**：指出改哪一行、改成什么（如"第 8 行 `range(1, n)` 在 n=1 时跳过，改为 `range(n)`"），附修改后片段。
- 算法思路错（如暴力 TLE）→ 不给完整答案，引导优化方向（"O(n²) 超时，考虑哈希表降到 O(n)"）。
- 代码完全偏离题意 → 重新解释题意核心逻辑，给代码骨架。

### 渐进提示系统（`/acm hint`）

对当前题目分**三层**逐步提示，每次只给一层，用户可逐层请求更深入。

**🚫 全程禁止贴该题的解题代码、循环结构、转移方程代码**——只给文字描述、思路、伪代码级别的方向。用户需要靠自己的理解把代码写出来。

| 层级 | 内容（均为文字/思路，非可运行代码） | 示例（200.岛屿数量） |
|------|------|----------------------|
| ① 方向 | 用什么算法/数据结构 | "用 BFS 或 DFS 遍历网格，连通区域标记已访问" |
| ② 方法 | 关键步骤/思路，更具体 | "从每个未访问的 '1' 启动 BFS，把相邻 '1' 标已访问，每启动一次就是一个岛屿" |
| ③ 细节 | 关键变量定义、边界条件、易错点（仍是文字） | "需要 visited 标记；BFS 用队列；注意网格边界判断；别忘了起点也要标记 visited" |

> 第③层给的是"**注意什么**"而非"**代码怎么写**"。例如二分题提示"while 用 <=、mid 用 //整除、找到要 break"，而不是贴出 while 循环代码。

用户未选题直接 `/acm hint` → "请先选择题目：用 `/acm quiz` 或 `/acm practice <题号>` 开始练习"。
用户说"给我答案/我不会了" → 才在对话框给完整解法（记录为"学习版"，不计入自主 AC）。

### 薄弱知识点追踪（`/acm review`）

每次评判后更新训练记录：`{题号, 标题, tags, score, verdict}`，按知识点累计 AC/总数。

生成报告：
- 按通过率升序列知识点，标注通过率（如"双指针 20%（1/5）"），推荐 2-3 道专项题。
- 首次训练无记录 → "暂无训练记录，完成几道题后即可生成分析"，推荐 Phase 1 入门题。
- 全部 AC → "目前表现优秀，全部通过！"，建议尝试更高难度或模拟赛。

报告同时给出整体统计：总作题数、AC 数、WA 数、跳过数。

---

## 输入输出模板

`templates/` 下提供 6 个模板，`/acm teach` 时按题目的 `inputMode` 推荐对应模板：

| inputMode | 模板 | 适用 |
|-----------|------|------|
| basic | `basic.py` | 单组数据（单值/一维数组） |
| multi | `multi_case.py` | 多组测试 |
| matrix | `matrix.py` | 矩阵 |
| graph | `graph.py` | 图（点+边） |
| tree | `tree.py` | 二叉树（层序/边） |
| — | `fast_io.py` | 大数据量快速读入 |

所有模板核心模式：
```python
import sys
input = sys.stdin.readline             # 快速读入
# ... 读取、计算 ...
# print() 输出结果
```
